name: Build MSYS2

on:
  push:
    branches:
      - master
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  build:
    name: Build Windows UCRT64
    runs-on: windows-2025

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up CPython
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Python packaging tools
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" -m pip install --upgrade pip build hatchling packaging meson ninja VapourSynth

      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-python
            mingw-w64-ucrt-x86_64-meson
            mingw-w64-ucrt-x86_64-ninja
            mingw-w64-ucrt-x86_64-pkgconf
            mingw-w64-ucrt-x86_64-boost
            mingw-w64-ucrt-x86_64-opencl-headers
            mingw-w64-ucrt-x86_64-opencl-icd

      - name: Prepare VapourSynth R77 wheel
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\ci_prepare_msys2.py

      - name: Build plugin
        run: /ucrt64/bin/python tools/ci_build_msys2.py --clean

      - name: Smoke-load artifact
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\smoke_load_artifact.py
          --vapoursynth-root _deps\vapoursynth-wheel-R77
          --artifact-dir dist\msys2-ucrt64
          --exercise-filter

      - name: Smoke-load artifact through manifest autoload
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\smoke_load_artifact.py
          --vapoursynth-root _deps\vapoursynth-wheel-R77
          --artifact-dir dist\msys2-ucrt64
          --autoload
          --exercise-filter

      - name: Pack local prebuilt zip
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\package_plugin_zip.py
          --input-dir dist\msys2-ucrt64
          --output dist\nnedi3cl-msys2-ucrt64.zip

      - name: Build wheel from local prebuilt package
        shell: cmd
        env:
          NNEDI3CL_PREBUILT_URL: ${{ github.workspace }}\dist\nnedi3cl-msys2-ucrt64.zip
        run: >-
          "%pythonLocation%\python.exe" -m build --wheel --no-isolation --outdir dist\wheels

      - name: Create release assets
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\ci_make_release_assets.py --clean

      - name: Smoke-load release zip
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\ci_smoke_package.py
          --artifact-zip dist\release-assets\nnedi3cl-msys2-ucrt64.zip
          --exercise-filter

      - name: Install built wheel
        shell: cmd
        run: >-
          for %%I in (dist\wheels\*.whl) do "%pythonLocation%\python.exe" -m pip install --force-reinstall "%%~fI"

      - name: Smoke test installed wheel
        shell: cmd
        run: >-
          "%pythonLocation%\python.exe" tools\ci_smoke_installed_wheel.py --exercise-filter

      - name: Smoke test source install with prebuilt release asset
        shell: cmd
        env:
          NNEDI3CL_PREBUILT_URL: ${{ github.workspace }}\dist\release-assets\nnedi3cl-msys2-ucrt64.zip
        run: |
          "%pythonLocation%\python.exe" -m pip uninstall -y vapoursynth-nnedi3cl
          "%pythonLocation%\python.exe" -m pip install --force-reinstall --no-deps --no-build-isolation -v .
          "%pythonLocation%\python.exe" tools\ci_smoke_installed_wheel.py --exercise-filter

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: nnedi3cl-msys2-ucrt64
          path: dist/msys2-ucrt64

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v7
        with:
          name: nnedi3cl-wheel-win64
          path: dist/wheels

      - name: Upload release assets artifact
        uses: actions/upload-artifact@v7
        with:
          name: nnedi3cl-release-assets
          path: dist/release-assets

      - name: Upload release asset
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'release'
        shell: powershell
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.release.tag_name || github.ref_name }}
        run: |
          $tag = $env:RELEASE_TAG
          $assets = Get-ChildItem dist\release-assets\* | Select-Object -ExpandProperty FullName
          & cmd.exe /d /c "gh release view $tag --repo $env:GITHUB_REPOSITORY >NUL 2>NUL"
          if ($LASTEXITCODE -eq 0) {
            gh release upload $tag $assets --clobber --repo $env:GITHUB_REPOSITORY
          } else {
            gh release create $tag $assets --title $tag --notes "Automated MSYS2/UCRT64 build." --repo $env:GITHUB_REPOSITORY
          }
